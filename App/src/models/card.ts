class Card {
    abbreviation: string;
    title: string;

    constructor(abbreviation: string, title:string) {
        this.abbreviation = abbreviation;
        this.title = title;
    }
}

export {Card}